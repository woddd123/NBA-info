# NBA 赛事数据助手 · iOS

SwiftUI 原生版，对应仓库根目录的 Vue PWA。最低支持 iOS 17。

## 打开和运行

1. 用 Xcode 打开 `NBAASS.xcodeproj`。
2. 选择任意 iPhone 模拟器。
3. 按 `⌘R` 运行。

项目使用 ESPN 公共接口获取赛程和排名；球队队徽来自 ESPN CDN。观赛链接和 BallDontLie API Key 只保存在本机。
